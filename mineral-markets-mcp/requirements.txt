# Runtime — instalar con: pip install -r requirements.txt
mcp[cli]>=1.2.0
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
yfinance>=0.2.40
tenacity>=8.2
cachetools>=5.3
numpy>=1.26

# Agente de monitoreo (monitor.py): persistencia, indicadores, config y dashboard
pandas>=2.2
rich>=13.7
pyyaml>=6.0
tzdata; sys_platform == "win32"   # zoneinfo necesita la base tz en Windows

# Dev / tests — instalar con: pip install -r requirements.txt -r requirements-dev.txt
# (ver requirements-dev.txt)
